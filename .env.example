# Frontend env vars (must be prefixed with VITE_ for Vite to expose them)

# In development: point to the local backend
VITE_API_URL=http://localhost:3001/api

# In production (Docker/Railway): leave empty — frontend uses /api (same origin)
# VITE_API_URL=
